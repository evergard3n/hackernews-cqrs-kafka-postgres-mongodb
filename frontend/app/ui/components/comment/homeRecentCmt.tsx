import { Comment } from "@/app/lib/definitions";

export default function HomeRecentComment({comments} : {comments: Comment[]}) {
    return (
        <div>
            this is a comment list
        </div>
    )
}